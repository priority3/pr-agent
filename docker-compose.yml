services:
  app:
    build: .
    container_name: pr-agent
    restart: unless-stopped
    ports:
      - '3030:3030'
    env_file:
      - .env
    environment:
      - PORT=3030
      - NODE_ENV=production
    volumes:
      # 单文件库 + 上传目录持久化(备份只需拷这个目录)
      - ./data:/app/data
